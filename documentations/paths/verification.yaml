# Verification paths
  /verifications:
    post:
      summary: create new verification request
      description: create new verification request
      operationId: createVerification
      tags:
      - Verifications
      parameters:
        - in: header
          name: Authorization
          description: Authentication token
          required: true
        - in: body
          name: payload
          description: verification details
          required: true
          schema:
            $ref: "#/definitions/Verification"
      responses:
        200:
          description: Success
          schema:
            $ref: "#/definitions/Verification"
    get:
      summary: fetch all verifications request(admin)
      description: fetch all verifications request
      operationId: fetchVerifications
      tags:
      - Verifications
      parameters:
        - in: header
          name: Authorization
          description: Authentication token
          required: true
      responses:
        200:
          description: Success
          schema:
            type: object
            properties:
              message:
                type: string
              data:
                type: array
                items:
                  $ref: "#/definitions/Verification"
  /verifications/{requestId}:
    get:
      summary: fetch verification(admin)
      description: fetch verification
      operationId: fetchVerification
      tags:
      - Verifications
      parameters:
        - in: header
          name: Authorization
          description: Authentication token
          required: true
      responses:
        200:
          description: Success
          schema:
            type: object
            properties:
              message:
                type: string
              data:
                type: object
                properties:
                schema:
                  $ref: "#/definitions/Verification"             
    put:
      summary: update verification request(admin)
      description: update verification
      operationId: updateVerification
      tags:
      - Verifications
      parameters:
        - in: header
          name: Authorization
          description: Authentication token
          required: true
        - in: body
          name: payload
          description: verification details
          required: true
          schema:
            $ref: "#/definitions/Verification"          
      responses:
        200:
          description: Success
          schema:
             $ref: "#/definitions/Verification"