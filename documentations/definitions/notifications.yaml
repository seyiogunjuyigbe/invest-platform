# Notifications
  Notification:
    type: object
    properties:
      title:
        type: string
      message:
        type: string
      user:
        type: object
      isRead:
        type: boolean
