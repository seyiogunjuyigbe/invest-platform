#Verification
  Verification:
    type: object
    properties:
      category:
        type: string
      documentNumber:
        type: string
      user:
        type: object
      document:
        type: string
      status: 
        type: string
        enum: ['pending', 'approved', 'declined']
      actionBy:
        type: object
      remarks:
        type: string
